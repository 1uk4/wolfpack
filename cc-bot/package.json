{
  "name": "wolfpack-cc-bot",
  "version": "0.1.0",
  "description": "Telegram command-center bot for the Wolfpack. Runs /status, /logs, /restart against wolves registered in ../inventory/hosts.yml.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "grammy": "^1.30.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
